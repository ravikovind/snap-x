# Brand assets — Open Notifier

Found by /snap-x on https://open-notifier.io (see the skill's "Brand assets" step):

| File | Origin |
|---|---|
| `favicon.svg` | https://open-notifier.io/favicon.svg (the site's own icon; uses text in Saira 900) |
| `icon.png` | `favicon.svg` rendered to 512×512 with Saira 900 (SVG text can't load webfonts when embedded as an `<img>`, so it's rasterised once) |

Logos and brand marks belong to their owners; used here only to demonstrate snap-x.
