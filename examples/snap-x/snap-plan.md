# snap-plan — snap-x (dogfood run of /snap-x)

Generated by following the /snap-x skill on this repository. Re-run any time: `npm run examples`.

## Step 1 — inspect (8-question rubric)
1. Name: snap-x
2. Description: Render self-contained Satori `.mjs` design files to PNG. Any format, any size. No browser.
3. Domain: github.com/ravikovind/snap-x
4. Tags: Node.js · Satori · resvg-js · MIT
5. Stack: Node.js, Satori, resvg-js, MCP
6. Font: Saira (weights 400/700/900) — the project's own hero images already use it
7. Accent: #eb1d25 on near-black #080808
8. Theme: dark

## Step 2 — plan
Hook: **Claude Code writes the design file; snap-x renders it.**

| Format | Size | Copy |
|---|---|---|
| readme-card | 1280×640 | `snap-x` wordmark · "Any format. Any size. One command. No browser. Pure Node.js." · pipeline: Claude writes design.mjs → snap-x check → snap-x render · install `npm install -g @snap-x/cli` — **used as the README header** |
| og | 1200×630 | wordmark + terminal mock: `npx @snap-x/cli render designs/*.mjs` |
| thumbnail | 1280×720 | "Social images, automated." + format list |
| cover | 1500×500 | wordmark + 3-step pipeline + stats (∞ formats · 0 browsers · 1 command) |
| poster | 1080×1920 | wordmark + stats + "add any .mjs file" format list |

Assets: none (no logo file) — the lucide `image-plus` icon is inlined as SVG paths in readme-card.
Glyphs: avoid ✔/emoji (Saira has none) — arrows (→) used instead.
