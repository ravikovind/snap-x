# snap-plan — snap-x (dogfood run of /snap-x)

Generated by following the /snap-x skill on this repository. Re-run any time: `npm run examples`.

## Step 1 — inspect (8-question rubric)
1. Name: snap-x
2. Description: Render self-contained Satori `.mjs` design files to PNG. Any format, any size. No browser.
3. Domain: github.com/ravikovind/snap-x
4. Tags: Node.js · Satori · resvg-js · MIT
5. Stack: Node.js, Satori, resvg-js, MCP
6. Font: Saira (weights 400/700/900) — the project's own hero images already use it
7. Accent: #eb1d25 on near-black #080808
8. Theme: dark

## Step 2 — plan
Hook: **Claude writes it. snap-x renders it.**  (direction: bold typographic, dark; red used only for emphasis)

Shared system: near-black `#070707`, red glow top-right, huge Saira 900 headline (two lines, "snap-x" in red), a small project → Claude Code → design.mjs → PNG strip, and the command in JetBrains Mono.

| Format | Size | Composition |
|---|---|---|
| readme-card | 1280×640 | brand bar · headline · one-line description · pipeline chips + command — **used as the README header** |
| og | 1200×630 | headline · full-width command bar ("any format → PNG") |
| thumbnail | 1280×720 | near-pure headline (122px) · large pipeline chips |
| cover | 1500×500 | headline + command left · numbered 4-step pipeline right (safe for X's center crop) |
| poster | 1080×1920 | four-line stacked headline · description · four pipeline rows (PNG highlighted) |

Assets: none (no logo file). Fonts: Saira 400/700/900 + JetBrains Mono 400/700.
Glyphs: avoid ✔/emoji (neither font has them) — → is used instead.
Lessons from the first render: sizes are set so headline lines never wrap (`whiteSpace: nowrap`, 164px on the poster, 122px on the thumbnail).
